# Dependencias del pipeline de descubrimiento de epitopos vacunales (pipeline.py).
# NOTA: torch/transformers/scikit-learn/matplotlib se retiraron junto con el
# motor de CNN de antigenicidad local (pivote a BepiPred-3.0, ver ADR en
# src/engines/bepipred_engine.py). No son requeridos por pipeline.py en si.
pandas>=1.5.0
numpy>=1.22.0

# NOTA: la Fase 2 (BepiPred-3.0) NO se instala aqui. Se ejecuta como
# subproceso local contra un paquete descargado manualmente por licencia
# academica (bepipred-3.0b.src/, gitignored). Sus dependencias (fair-esm,
# torch==1.12.0, plotly, ...) son antiguas y pueden chocar con las de este
# archivo: instalalas en un venv APARTE con
# 'pip install -r bepipred-3.0b.src/BepiPred3_src/requirements.txt' y apunta
# BEPIPRED_PYTHON_BIN al python de ese venv. Ver README.md - Seccion de
# Instalacion.
#
# NOTA: la Fase 4 (BLASTp local) depende del binario 'blastp' (paquete NCBI
# BLAST+), que NO se instala via pip. Ver README.md - Seccion de Instalacion.
#
# NOTA: la Fase 5 (NetMHCIIpan-4.3, promiscuidad T-helper MHC-II) depende del
# script/binario propietario 'netMHCIIpan' (licencia academica DTU Health
# Tech, gitignored en 'netMHCIIpan-4.3/'), tampoco distribuible via pip. El
# wrapper del binario requiere el interprete 'tcsh' instalado en el sistema
# (paquete de SO, no pip: 'apt-get install tcsh' en Debian/Ubuntu). Ver
# README.md - Seccion de Instalacion.
